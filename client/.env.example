# Clerk Authentication
VITE_CLERK_PUBLISHABLE_KEY="pk_test_..."

# Backend URL
# Leave empty for Vercel Monorepo deployment (uses relative path /api)
# Set to full URL (e.g., https://api.example.com) if backend is hosted separately
VITE_API_URL=""
